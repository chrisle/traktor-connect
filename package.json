{
  "name": "traktor-connect",
  "version": "1.0.0",
  "description": "Library to receive track metadata from Traktor Pro via OGG Vorbis broadcast",
  "license": "MIT",
  "private": false,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "tsc --build tsconfig.json",
    "watch": "tsc --build tsconfig.json -w",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build && npm test",
    "release": "node .github/release.js",
    "release:patch": "node .github/release.js --patch",
    "release:major": "node .github/release.js --major"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
